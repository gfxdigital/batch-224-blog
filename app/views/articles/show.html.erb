<%= cl_image_tag @article.photo, height: 100, width: 100, crop: :fill, gravity: :face %>

<%= image_tag @article.photo.url(:bright_face) %>

<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @article.body %>
</p>

<%= link_to 'Edit', edit_article_path(@article) %> |
<%= link_to 'Back', articles_path %>
